# Railway con Volume persistente montado en /data
DATABASE_PATH=/data/database.db

# Vercel frontend → URL del backend en Railway
# ALLOWED_ORIGINS=https://ns-dash.vercel.app,http://localhost:3000

# Local (sin Volume)
# DATABASE_PATH=app/database.db
