# Railway (producción): agregar PostgreSQL al proyecto y vincular DATABASE_URL al servicio del backend.
# DATABASE_URL=postgresql://...

# Vercel frontend → URL del backend en Railway
# ALLOWED_ORIGINS=https://ns-dash.vercel.app,http://localhost:3000

# Solo local (SQLite). No usar en Railway.
# DATABASE_PATH=app/database.db
